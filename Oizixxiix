#include <iostream>
#include <map>
#include <fstream>

int main() {
  std::map<std::string, std::string> dictionary; // создаем объект map для хранения слов

  // открываем файл для записи
  std::ofstream outputFile("slovar.txt");
  
  // добавляем несколько примеров слов в словарь
  dictionary["hello"] = "привет";
  dictionary["apple"] = "яблоко";
  dictionary["cat"] = "кот";

  bool running = true;
  while (running) {
    std::cout << "Выберите действие:" << std::endl;
    std::cout << "1. Ввести английское слово и получить русский перевод" << std::endl;
    std::cout << "2. Ввести русское слово и получить английский перевод" << std::endl;
    std::cout << "3. Завершить программу" << std::endl;

    int choice;
    std::cin >> choice;

    switch (choice) {
      case 1: {
        std::string word;
        std::cout << "Введите английское слово: ";
        std::cin >> word;

        if (dictionary.count(word) > 0) {
          std::cout << "Русский перевод: " << dictionary[word] << std::endl;
        } else {
          std::cout << "Перевод для данного слова не найден." << std::endl;
        }
        break;
      }
      case 2: {
        std::string word;
        std::cout << "Введите русское слово: ";
        std::cin >> word;

        // ищем слово в словаре и выводим его английский перевод
        for (const auto& pair : dictionary) {
          if (pair.second == word) {
            std::cout << "Английский перевод: " << pair.first << std::endl;
            break;
          }
        }
        std::cout << "Перевод для данного слова не найден." << std::endl;
        break;
      }
      case 3: {
        running = false;
        break;
      }
      default: {
        std::cout << "Неверный выбор. Попробуйте снова." << std::endl;
        break;
      }
    }
  }

  // закрываем файл после использования
  outputFile.close();

  return 0;
}


В данном коде мы создаем объект map с названием dictionary, который хранит английские слова в виде ключей и их русские переводы в виде значений. Мы добавляем несколько примеров слов в словарь и затем начинаем бесконечный цикл для работы с пользователем.

Пользователь может выбрать одно из трех действий: ввести английское слово и получить русский перевод, ввести русское слово и получить английский перевод, или завершить программу.

Когда пользователь выбирает одно из первых двух действий, мы запрашиваем у него соответствующее слово и ищем его в словаре. Если слово найдено, мы выводим соответствующий перевод, а если нет - сообщаем пользователю, что перевод не найден.

При выборе третьего действия, программа завершается, и после этого мы записываем все слова из словаря в файл "slovar.txt" с помощью объекта ofstream.

Обратите внимание, что данный код не проверяет, повторяются ли уже добавленные слова. Если вы хотите добавить эту функциональность, вам нужно будет внести соответствующие изменения в код.
очиститьОчистить чат
СтопСтоп
Какой код написать или проверить?

.
.
.
Какой код написать или проверить?

.
.
.
